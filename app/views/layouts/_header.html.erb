<header class="dropdown-header">
  <div class="container">
    <nav>
      <ul class="nav navbar-nav navbar-right">
        <% if logged_in? %>
            <li><%= link_to "Home", home_path, :class => 'btn btn-primary-outline' %></li>
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                Account <b class="caret"></b>
              </a>
              <ul class="dropdown-menu">
                <li><%= link_to "Profile", currentdev_user, :class => 'btn btn-primary-outline' %></li>
                <li class="divider"></li>
                <li>
                  <%= link_to "Log out", logout_path, method: "delete", :class => 'btn btn-primary-outline' %>
                </li>
              </ul>
            </li>
        <% else %>
            <li><%= link_to "Sign in", login_path, :class => 'btn btn-primary-outline' %></li>
            <li><%= link_to "Sign up", new_user_path, :class => 'btn btn-primary-outline' %></li>
        <% end %>
      </ul>
    </nav>
  </div>
</header>
